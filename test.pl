/* SWI-Prolog query log.  This file contains all syntactically
   correct queries issued in this directory.  It is used by the
   test wizard to generate unit tests.
*/

parse('.'(look,[]),action(player,look)).
halt.
